node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.vercel
.playwright-mcp/
.env*
!.env.example
.remember/
SETUP.txt
SUPABASE.txt

.vercel
.env*

# Supabase CLI scratch
supabase/.temp/

# Screenshots from scripts/shots.mjs. Evidence for a human, not source.
shots/

# Agent test harnesses and screenshots. Scratch, not source.
.testing/

# Screenshots left behind by browser-driving agents. They belong in a scratch
# directory, never in the repo root, and never in THIS repo, which is not the
# one they were taken of.
/*.png
